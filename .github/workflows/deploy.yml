name: Deploy FreshCart Pipeline to Snowflake

on:
  push:
    branches: [main]
    paths:
      - 'sql/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Snowflake CLI
        run: pip install snowflake-cli-labs

      - name: Write private key to file
        run: |
          cat > /tmp/rsa_key.p8 << 'KEYEOF'
          ${{ secrets.SNOWFLAKE_PRIVATE_KEY }}
          KEYEOF
          sed -i 's/\r$//' /tmp/rsa_key.p8
          chmod 0600 /tmp/rsa_key.p8

      - name: Validate private key format
        run: |
          openssl pkey -in /tmp/rsa_key.p8 -noout -text > /dev/null && echo "Key format OK"

      - name: Configure Snowflake CLI connection
        run: |
          mkdir -p ~/.snowflake
          cat > ~/.snowflake/config.toml << EOF
          [connections.freshcart]
          account = "${{ secrets.SNOWFLAKE_ACCOUNT }}"
          user = "${{ secrets.SNOWFLAKE_USER }}"
          role = "${{ secrets.SNOWFLAKE_ROLE }}"
          warehouse = "${{ secrets.SNOWFLAKE_WAREHOUSE }}"
          authenticator = "SNOWFLAKE_JWT"
          private_key_file = "/tmp/rsa_key.p8"
          EOF
          chmod 0600 ~/.snowflake/config.toml

      - name: Deploy SQL files in order
        run: |
          for f in sql/00_environment_setup.sql sql/01_stage_layer.sql sql/02_clean_layer.sql \
                   sql/03_dq_checks.sql sql/04_consumption_layer.sql sql/05_task_orchestration.sql \
                   sql/06_reporting_views.sql; do
            echo "Running $f"
            snow sql -f "$f" -c freshcart || exit 1
          done